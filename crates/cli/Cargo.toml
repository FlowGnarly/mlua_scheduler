[package]
name = "mlua_scheduler_cli"
version = "2.0.0"
edition = "2021"

[dependencies]
mlua = { version = "0.10.0", features = ["luau", "async", "send"] }
smol = "2.0.2"

mlua_scheduler = { version = "2.0.0", path = "../scheduler" }

clap = { version = "4.5.20", features = ["derive"] }
